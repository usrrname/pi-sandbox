{
  "name": "pi-sandbox",
  "scripts": {
    "start": "varlock run -- && make up",
    "build": "varlock run -- && make build"
  },
  "dependencies": {
    "@varlock/1password-plugin": "1.2.0",
    "varlock": "^1.1.0"
  }
}
